from io import UnsupportedOperation


def agregar_una_vez(lista, el):
    lista = []
    el = usuario
    usuario = input("Introduce un elemento: ")
    if el == lista:
        print("ValueError")
    else:
        lista.append(el)
